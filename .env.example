# Server configuration
PORT=3001
# Comma-separated origins allowed to post to the API. Leave blank to allow all origins.
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# SMTP configuration
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_USER=apikey-or-username
SMTP_PASS=super-secret-password

# Email address that should receive contact form submissions.
# Defaults to SMTP_USER if not provided.
CONTACT_RECIPIENT=inquiries@crisismgmt.net
